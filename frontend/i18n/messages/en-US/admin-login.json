{
  "groups": {
    "loginPage": {
      "title": "Sign-in page",
      "description": "Control the browser title for the sign-in page and the default destination after sign-in."
    },
    "loginSecurity": {
      "title": "Sign-in security",
      "description": "Control token lifetime, failed sign-in lockouts, and platform HTTP 429 rate limiting."
    }
  },
  "fields": {
    "loginDefaultNextPath": {
      "label": "Default redirect path",
      "description": "Default destination after sign-in when /login has no next parameter."
    },
    "tokenTTLHours": {
      "label": "Access token lifetime",
      "description": "Access Token lifetime in hours. Range: 1-168."
    },
    "refreshTokenTTLHours": {
      "label": "Keep signed in",
      "description": "Refresh Token and session lifetime in hours. Range: 1-8760."
    },
    "rateLimitEnabled": {
      "label": "Platform rate limit",
      "description": "When enabled, the platform returns HTTP 429. Disable it when Traefik, a gateway, or another outer layer handles rate limiting."
    },
    "rateLimitRPM": {
      "label": "User API rate limit",
      "description": "Base per-minute request limit for signed-in user APIs. It has no effect when platform rate limiting is disabled."
    },
    "publicAuthRateLimitRPM": {
      "label": "Public auth rate limit",
      "description": "Base per-minute request limit for public login, registration, and token refresh APIs. It has no effect when platform rate limiting is disabled."
    }
  },
  "validation": {
    "defaultNextPath": "The default redirect path must be an internal path, for example /chat."
  },
  "toast": {
    "sessionExpired": "Session expired",
    "signInAgain": "Please sign in again before continuing.",
    "loadFailed": "Failed to load authentication settings",
    "saveFailed": "Could not save authentication settings",
    "settingsUpdated": "Authentication settings updated"
  }
}
